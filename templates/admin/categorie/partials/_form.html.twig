
<div class="w-[300px] mx-auto mt-10">
{{ form_start(form) }}

   <div > {{ form_widget(form.img) }}</div>
    <div class="mt-5">{{ form_widget(form.titre) }}</div>
    <div class="mt-5">{{ form_widget(form.description) }}</div>
    
    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mt-5">{{ form_widget(form.update) }}</button>
{{ form_end(form) }}
       
</div>